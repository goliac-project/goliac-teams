admin_team: admin

enable_rulesets: true
rulesets:
  - default

max_changesets: 50

destructive_operations:
  repositories: true
  teams: false
  users: false
  rulesets: false

usersync:
  plugin: noop

workflows:
- standard
- noop
- repo
